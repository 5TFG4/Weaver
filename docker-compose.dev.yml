version: '3.8'
services:
  weaver_dev:
    build: .
    ports:
      - "${HOST_PORT}:${CONTAINER_PORT}"
    volumes:
      - .:/weaver
      - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro
    env_file:
      - .env
