HOST_PORT=8000
FRONTEND_PORT=80

POSTGRES_PROD_PORT=5432
POSTGRES_DB=weaverdb
POSTGRES_USER=user
POSTGRES_PASSWORD=password

# =============================================================================
# Alpaca API Configuration
# =============================================================================
# Production environment needs BOTH live and paper credentials.
# They can run IN PARALLEL (not just time-based switching):
#   - Process A: Live trading with real money
#   - Process B: Backtesting / training with paper account (simultaneously)

# Live Trading (Real Money)
ALPACA_LIVE_API_KEY=your_live_api_key
ALPACA_LIVE_API_SECRET=your_live_api_secret
ALPACA_LIVE_BASE_URL=https://api.alpaca.markets

# Paper Trading (Simulation / Backtest)
ALPACA_PAPER_API_KEY=your_paper_api_key
ALPACA_PAPER_API_SECRET=your_paper_api_secret
ALPACA_PAPER_BASE_URL=https://paper-api.alpaca.markets
